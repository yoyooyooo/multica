# Fork generation v0.4.22

## Status and authority

**Current formal generation** under the rule “formal = latest synced upstream version.”

Publication state at this document revision: **local formal candidate**. Treat `fork/v0.4.22` as the single current formal *identity*, not as a published/CI-green deployable head, until origin carries the branch tip and exact-head required checks exist as separate receipts.

- generation branch: `fork/v0.4.22`
- frozen upstream base: official tag `v0.4.22@8bd49bba8…` (changelog tip of the tag line)
- replay method: `git rebase --onto v0.4.22 <previous-freeze-base>` of fork-only tip commits from the prior generation
- prior live generation: `fork/upstream-main-20260808` / `b3118a340` (nearest tag `v0.4.21`)
- older rollback: `fork/v0.4.12@d5ec9569e`

History shape (6 fork-only tip commits on official tag):

```text
[v0.4.22 upstream @ 8bd49bba8]
610637f21 jackie  ci(fork): bootstrap upstream-main generation checks
cfa91eb18 jackie  feat(fork): replay generation on upstream main
772150304 jackie  ci(mobile): verify upstream-main generation
4469efb5e jackie  fix(fork): remediate access grant review findings
5f5498229 jackie  docs(fork): set active generation to latest synced upstream line
66fdc1d5b jackie  docs(fork): declare fork/v0.4.22 as active generation on official tag  <-- formal tip
```

Fork migrations renumbered to `272`–`291` so they sit after upstream’s `271` ceiling on this tag. Formal migration ceiling filename: `291_workload_pr_merge_delegation_event_history_index`.

## Claim limit

This document is the **single current formal generation home**. It records generation identity and intended deploy authority only. It does **not** claim:

- origin publication of `fork/v0.4.22`;
- exact-head required CI green;
- image digests or dual-host runtime readback;
- migration dry-run safety for live mini/imile ledgers.

Those remain separate receipts. Prior live tip `b3118a340` / `fork/upstream-main-20260808` stays rollback inventory until redeploy is separately evidenced under Program B (`~/.ags-cli/docs`).
