# Fork generation v0.4.22

## Status and authority

**Current formal generation** under the rule “formal = latest synced upstream version.”

Deployment state at this document revision: Mini runs exact accepted code revision `f219f451300ef8fe86ea01f3ac65c0f53c7c27ae` (`v0.4.22-30-gf219f4513`) under immutable tag `fork-mini-v0.4.22-r1`. Exact-head CI run [31724265369](https://github.com/yoyooyooo/multica/actions/runs/31724265369) succeeded. The generation branch may advance past that tag for receipt-only documentation; such commits do not change Mini runtime authority. This does not imply imile runtime convergence.

- generation branch: `fork/v0.4.22`
- frozen upstream base: official tag `v0.4.22@8bd49bba8…` (changelog tip of the tag line)
- replay method: `git rebase --onto v0.4.22 <previous-freeze-base>` of fork-only tip commits from the prior generation
- mini immutable deployment tag: `fork-mini-v0.4.22-r1` (tag object `ac8419a30114f06d757a9157f11e1b053d9687ab`, peeled commit `f219f451300ef8fe86ea01f3ac65c0f53c7c27ae`)
- prior live generation: `fork/upstream-main-20260808` / `b3118a340` (nearest tag `v0.4.21`)
- older rollback: `fork/v0.4.12@d5ec9569e`

History shape began with the declared generation replay below and has since advanced linearly to 30 fork-only commits on the official tag:

```text
[v0.4.22 upstream @ 8bd49bba8]
610637f21 jackie  ci(fork): bootstrap upstream-main generation checks
cfa91eb18 jackie  feat(fork): replay generation on upstream main
772150304 jackie  ci(mobile): verify upstream-main generation
4469efb5e jackie  fix(fork): remediate access grant review findings
5f5498229 jackie  docs(fork): set active generation to latest synced upstream line
66fdc1d5b jackie  docs(fork): declare fork/v0.4.22 as active generation on official tag
745663aa4 jackie  docs(fork): listen fork/v0.4.22 CI and mark formal tip unpublished
8e7367eb8 jackie  ci(fork): skip path filter on generation branch push
... source-accepted T016–T018, local-runtime, External PR compatibility and reconciliation deltas ...
f219f4513 jackie  fix(server): guard Lark backfills without database  <-- deployed formal tip
```

Fork migrations renumbered to `272`–`291` so they sit after upstream’s `271` ceiling on this tag.

当前 generation 的 External PR association compatibility 接受并严格校验 AGS 现行完整
projection envelope，修复 strict JSON decoder 对新增展示/绑定字段直接返回 HTTP 400 的问题。
这些字段只用于关联兼容与 fail-closed wire 校验；本 generation 不持久化 provider binding
作为 Multica authority，也不声明 `pr.merge` delegation 已上线。

该兼容 delta 是本 generation 的 fork-only 变更，不来自 donor 或 prior generation：

- `180dde82f` — 接受完整 AGS projection envelope，并将其纳入 idempotency hash；
- `47617bd3c` — review repair：拒绝带首尾空白的非 canonical repository wire 值。

文件清单：

- `server/internal/handler/external_pr_integration.go`
- `server/internal/handler/external_pr_integration_test.go`
- `docs/features/fork/external-pr-integration/README.md`
- `docs/releases/fork-generations/v0.4.22.md`

T016 adds forward-only retirement migrations `292`–`299` that drop dead `workspace_workload_authority` / `workload_pr_merge_delegation*` objects. T017 adds `300`–`301` External PR core simplification. Silent-continuation reconciliation adds `302`–`316`; the current ceiling is `316_external_pr_reconcile_finalization_primary_key`. Rollback across 299/301 or the 302–316 durable-reconcile schema requires the accepted pre-switch dump, not `migrate down`.

## Accepted Mini deployment

Mini was switched on 2026-08-14 to exact source `f219f451300ef8fe86ea01f3ac65c0f53c7c27ae` using immutable tag `fork-mini-v0.4.22-r1`:

- backend local image ID: `sha256:c41bafa57866eef367ba9e2c853aae9ba87f02a48400d1725672efe92dfa1699`;
- frontend local image ID: `sha256:bd9ce9511e2a0136749157e17e3eaa7fa9ac5298c9e345fbd4540e20a5c09ded`;
- both images carry revision `f219f4513`, version `v0.4.22-30-gf219f4513`, and deployment label `fork-mini-v0.4.22-r1`;
- CLI/daemon report the same version; backend `/readyz` reports DB and migrations `ok`; frontend browser login render passed;
- existing `multica_pgdata` and `multica_default` identities were preserved; migration ledger remained byte-for-byte unchanged at ceiling 316;
- uploads now bind to the main checkout. Six files stranded in an orphaned deleted-worktree mount were recovered without overwrite and verified as part of the 682-file host/container manifest;
- `external_pr_reconcile` scheduler executions succeeded after the switch.

Owner-only deployment receipt:

```text
/Users/yoyo/.local/state/multica/deployments/20260813T232320Z-fork-v0.4.22-f219f4513-mini/deployment-receipt.json
SHA-256: af374e3fde014c0a280c1380ee35bedca3b763fd5d1d48d0a06d949f32dbab6e
```

The local image IDs are content-addressed Mini facts; no registry push or registry digest publication is claimed.

## Claim limit

This document is the **single current formal generation home**. Source acceptance, exact-head CI, immutable deployment tag, local artifacts and Mini runtime each retain their own evidence. It does **not** claim:

- imile or any other host has converged to this generation;
- registry-published image digests exist for the Mini-local images;
- Lark `union_id` enrichment works without the external provider contact permission.

## Source deltas after the accepted Mini tag

`Dockerfile.web` now vendors the Mini r2 offline `next/font/google` mock so later frontend image builds do not fetch Google Fonts at compile time. This is source-only until the next accepted frontend image. Capability: [Offline Google Fonts for web image builds](../../features/fork/offline-google-fonts/README.md).

Prior live tip `b3118a340` / `fork/upstream-main-20260808` remains immutable rollback inventory; it is no longer Mini runtime authority.
