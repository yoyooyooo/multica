# Fork generation v0.4.12

## State

Candidate. Source acceptance, deployment, and live runtime verification are not yet claimed.

## Frozen sources

| Role | Ref | Exact commit |
| --- | --- | --- |
| Official upstream release | `v0.4.12` | `e52b50658a66d09bffed126e34116ad826c03623` |
| Previous accepted generation | `fork/v0.4.9` | `d5bed4d1f566ff8327637d0ad188398fac77eb08` |
| Previous accepted source tree | — | `324f115502ff022e6cb4e5841704e32fe92416f6` |

The candidate branch starts directly from the official release tag. It does not merge, rebase, or rewrite `fork/v0.4.9`.

## Delta inventory

| Delta | Classification | Candidate treatment |
| --- | --- | --- |
| Provider-neutral external PR integration | rework | Replay the accepted v0.4.9 contract on the v0.4.12 VCS and Issue surfaces. |
| Exact `record_only` completion policy | keep | Preserve fail-closed policy evaluation and supported public completion/read paths. |
| Purpose-bound workload assertions | keep | Preserve deployment-unique issuer, Task provenance, Team authority, and canonical signed envelope. |
| Workspace workload authority projection | rework | Preserve authority semantics while converging the v0.4.12 migration line. |
| Historical fork migration convergence | rework | Keep old ledgers immutable and add a forward-only v0.4.12 bridge. |
| Fork CI, Helm, Compose, self-host and bridge checks | rework | Point generation checks at `fork/v0.4.12` and retain upstream v0.4.12 CI behavior. |
| `fork/v0.4.8` implementation history | retire from current source | Retain only as historical fixture/donor evidence. |
| `fork/v0.4.9` implementation history | previous generation | Retain as exact rollback source and upgrade fixture. |

## Migration boundary

Official v0.4.12 owns migrations `224`–`230`. The previous fork generation already deployed different migration names under `224`–`236`; those historical files and ledger rows are not renamed or edited.

This generation assigns the fork reconciliation sequence new unique prefixes `231`–`243`. The SQL is intentionally additive/idempotent so both paths converge:

1. clean official v0.4.12 database: official `001`–`230`, then fork `231`–`243`;
2. deployed fork/v0.4.9 database: historical fork ledger retained, official v0.4.12 `224`–`230` applied by exact filename, then fork `231`–`243` reapplied as reconciliation;
3. retained historical-135 and fork/v0.4.8 fixtures: forward-converged through the same current sequence.

Migration `241_external_pr_index_reconciliation_fence` is forward-only. Rollback across it requires restoration of an accepted pre-v0.4.12 database backup rather than migration down.

## Required source evidence

Before source acceptance:

- migration prefix lint and matching up/down files;
- clean-v0.4.12, historical-135, fork/v0.4.8, and deployed-fork/v0.4.9 migration matrix;
- focused completion-policy, External PR, workload assertion, authority, router, Helm, Compose, and installer tests;
- full Go tests, TypeScript tests/typecheck/build/lint, generated-file checks, and clean diff checks;
- exact-head CI on a PR targeting `fork/v0.4.12`;
- independent whole-diff review with no unresolved P0–P2 findings.

## Deployment contract

Deployment is allowed only after source acceptance. Backend and frontend must be built from the same exact accepted head and recorded by immutable image digests.

The Mini target deployment must:

- set `MULTICA_APP_URL`, `FRONTEND_ORIGIN`, and `MULTICA_PUBLIC_URL` to `https://mini.tail9146e0.ts.net:37445`;
- retain the deployment-unique workload issuer and issuer instance ID;
- preserve the existing PostgreSQL container, network, and volumes;
- take an owner-controlled database backup before migration;
- replace only backend and frontend containers;
- verify health, readiness, migration ledger, login, workload assertion exchange, and a disposable AGS/Forgejo PR narrative using the `37445` Issue URL.

## Rollback source

Until this generation is deployed and accepted, the rollback source remains:

- source: `fork/v0.4.9@d5bed4d1f566ff8327637d0ad188398fac77eb08`;
- backend image: `sha256:ad391f8507852827d1bff09f75d5de82813935044432af3553257991de6ad15a`;
- deployment receipt: `/Users/yoyo/.local/state/multica/deployments/20260728T005902Z-context-continuity-d5bed4d-retry/deployment-receipt.txt`.

The currently running older frontend image is operational evidence, not accepted generation source.

## Claim limit

Not yet claimed:

- accepted `fork/v0.4.12` source head;
- CI or review completion;
- deployable backend/frontend artifacts;
- production database upgrade;
- runtime activation;
- post-deployment login or provider projection proof.
